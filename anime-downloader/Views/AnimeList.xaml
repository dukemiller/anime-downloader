<UserControl x:Class="anime_downloader.Views.AnimeList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="330" d:DesignWidth="650">
    <Grid>
        <DataGrid x:Name="DataGrid"
                  AutoGenerateColumns="False"
                  CanUserResizeRows="False"
                  CanUserAddRows="False"
                  UseLayoutRounding="False"
                  SelectionUnit="Cell" IsReadOnly="True"
                  FocusVisualStyle="{x:Null}"
                  RowHeaderWidth="0"
                  HorizontalContentAlignment="Center"
                  GridLinesVisibility="None" BorderThickness="0" FontSize="16" >
            <DataGrid.ContextMenu>
                <ContextMenu Name="AnimeContextMenu">
                    <MenuItem Name="Add" Header="Add ..." />
                    <MenuItem Name="Edit" Header="Edit ..." />
                    <MenuItem Name="Delete" Header="Delete ..." />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="350" Binding="{Binding Path=Title}" />
                <DataGridTextColumn Header="Episode" Binding="{Binding Path=Episode}" />
                <DataGridTextColumn Header="Status" Width="80" Binding="{Binding Path=Status}" />
                <DataGridTextColumn Header="Airing" Width="auto" Binding="{Binding Path=Airing}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>