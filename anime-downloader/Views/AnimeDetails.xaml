<UserControl x:Class="anime_downloader.Views.AnimeDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="680">
    <UserControl.Resources>
        <Style x:Key="RadioStyle" TargetType="{x:Type RadioButton}">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="{x:Type Label}">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        </Style>
        <Style x:Key="ValueStyle" TargetType="Control">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.Background>White</Grid.Background>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="3*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Grid x:Name="TextboxGrid" Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="11*" />
                    </Grid.ColumnDefinitions>

                    <Label x:Name="NameLabel" Grid.Row="0" Grid.Column="0" Content="Name: " VerticalAlignment="Center"
                   HorizontalAlignment="Left" FontSize="16" FontFamily="Yu Gothic"
                   ToolTip="A hopefully close approximation to the name of the show." />
                    <TextBox x:Name="NameTextbox" Grid.Column="1" Grid.Row="0" Height="27" TextWrapping="Wrap" Text=""
                     VerticalAlignment="Center" FontSize="18.667" GotFocus="NameTextbox_GotFocus" />
                    <Label x:Name="EpisodeLabel" Content="Episode: " VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="Yu Gothic"
                   FontSize="16"
                   ToolTip="The last episode you've watched."
                   Grid.Column="0" Grid.Row="1" />
                    <TextBox x:Name="EpisodeTextbox" Grid.Column="1" Grid.Row="1" Height="27" TextWrapping="Wrap" Text="00"
                     VerticalAlignment="Center" FontSize="18.667" GotFocus="EpisodeTextbox_GotFocus"
                     PreviewTextInput="EpisodeTextbox_PreviewTextInput" />
                </Grid>
                <Grid x:Name="CheckboxContainerGrid" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="CheckboxGridLeft" Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>

                        <!-- Status -->
                        <Label x:Name="StatusLabel" Grid.Row="0" Grid.Column="0" Content="Status: "
                       Style="{DynamicResource LabelStyle}"
                       ToolTip="What state you're currently at in regards to watching the show." />
                        <Grid x:Name="StatusContainerGrid" Grid.Row="0" Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <RadioButton x:Name="StatusFinishedRadio" Grid.Column="0" Content="Finished"
                                 Style="{DynamicResource RadioStyle}" />
                            <RadioButton x:Name="StatusWatchingRadio" Grid.Column="1" Content="Watching"
                                 Style="{DynamicResource RadioStyle}" IsChecked="True" />
                            <RadioButton x:Name="StatusOnHoldRadio" Grid.Column="2" Content="On Hold"
                                 Style="{DynamicResource RadioStyle}" />
                            <RadioButton x:Name="StatusDroppedRadio" Grid.Column="3" Content="Dropped"
                                 Style="{DynamicResource RadioStyle}" />
                        </Grid>

                        <!-- Resolution -->
                        <Label Grid.Row="1" Grid.Column="0" x:Name="ResolutionLabel" Content="Resolution: "
                       Style="{DynamicResource LabelStyle}" ToolTip="The quality that will be downloaded." />
                        <Grid Grid.Column="1" Grid.Row="1" x:Name="ResolutionContainerGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <RadioButton x:Name="Resolution480" Grid.Column="0" Content="480"
                                 Style="{DynamicResource RadioStyle}" />
                            <RadioButton x:Name="Resolution780" Grid.Column="1" Content="720"
                                 Style="{DynamicResource RadioStyle}" IsChecked="True" />
                            <RadioButton x:Name="Resolution1080" Grid.Column="2" Content="1080"
                                 Style="{DynamicResource RadioStyle}" />
                        </Grid>

                        <!-- Subgroup -->
                        <Label x:Name="SubgroupLabel" Grid.Row="2" Grid.Column="0" Content="Only from:"
                       Style="{DynamicResource LabelStyle}"
                       ToolTip="Only download from animes that have this subgroup." />
                        <ComboBox x:Name="SubgroupComboBox" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left"
                          VerticalAlignment="Center" Width="90" Height="25">
                            <ComboBoxItem IsSelected="True" Content="(None)" />
                        </ComboBox>

                        <!-- Rating -->
                        <Label x:Name="RatingLabel" Grid.Row="3" Grid.Column="0" Content="Rating: "
                       Style="{DynamicResource LabelStyle}" ToolTip="What you would rate the show (out of 10?)" />
                        <TextBox x:Name="RatingTextbox" Grid.Row="3" Grid.Column="1" Height="25" Width="60"
                         HorizontalAlignment="Left" FontSize="18.667" GotFocus="EpisodeTextbox_GotFocus"
                         PreviewTextInput="RatingTextbox_PreviewTextInput" />

                        <!-- Airing -->
                        <Label x:Name="AiringLabel" Grid.Row="4" Grid.Column="0" Content="Airing: "
                       Style="{DynamicResource LabelStyle}"
                       ToolTip="Whether the anime is currently airing and will be included on the search for new episodes." />
                        <CheckBox x:Name="AiringCheckbox" Grid.Row="4" Grid.Column="1" Style="{DynamicResource ValueStyle}"
                          IsChecked="True" />

                        <!-- Name Strict -->
                        <Label x:Name="NameStrictLabel" Grid.Row="5" Grid.Column="0" Content="Name Strict: "
                       Style="{DynamicResource LabelStyle}"
                       ToolTip="Whether searching for this anime should match exactly what the name is instead of picking up partial matches." />
                        <CheckBox x:Name="NameStrictCheckbox" Grid.Row="5" Grid.Column="1" Style="{DynamicResource ValueStyle}" />
                    </Grid>
                    <Grid x:Name="CheckboxGridRight" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </Grid>
            </Grid>
            <Button x:Name="OpenLastButton" Grid.Row="1" Content="Last episode" Style="{DynamicResource ConfirmationButtonStyle}" Width="auto" HorizontalAlignment="Left" />
            <Button x:Name="AddAnimeButton" Grid.Row="1" Content="Add" Style="{DynamicResource ConfirmationButtonStyle}" HorizontalAlignment="Right" />
        </Grid>
    </Grid>
</UserControl>